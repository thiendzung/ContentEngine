# AI_context.MD - ContentEngine shared working window

Updated: 2026-09-12. Exact canonical filename: `AI_context.MD`.

## Read and synchronize first

Inspect local changes -> fetch origin -> synchronize the exact assigned ref without destructive reset -> require clean tree and matching SHA -> read local `AGENTS.md`, this file, `docs/TASKS.md`, `docs/CHECKLIST.md` and the exact task. GitHub live refs/PRs/CI are dynamic truth; do not reuse old chat status.

## Objective

M1: ONE REAL JOURNAL PASS on the Founder's local runtime, then M2 repeatability, M3 placement/measurement, M4 evidence-led learning. Contract: `docs/20-LOCAL-FIRST-DELIVERY-SPEC.md`. Schedule: `docs/PLAN.md`.

The local-first planning change does NOT prove a runtime fix or complete M1. It becomes effective only on Founder merge. Founder dispatches tasks by copy; MG owns architecture/coding/review; Agent Local executes the exact local task; Founder alone merges and makes final editorial decisions.

## Current observed gate

M1: IN PROGRESS. Last reported blocker: authorized Angle execution rejected by a local execution safety gate before a model call. Exact rejecting layer/root cause is NOT yet verified. Treat it as a diagnostic question, not proof of a code bug.

The fresh candidate has prior evidence research (8 supports / 3 readable documents), but final EvidenceSet/OriginalityPack identifiers, hashes, approval/lock and exact bundle bindings require one read-only local verification. If valid, do not research again.

Canonical observed runtime identity and full lineage:

`docs/logs/2026-09-12-ce05-m1-one-real-journal-pass-status.md`

Expected runtime from that report: local `contentengine`, compose project `contentengine`, migration `20260910_0022`. Re-verify; do not assume rows exist from a Markdown note. Do not search historical databases again or recreate old rows without an explicit new recovery decision.

## Next exact local task

TASK: LF-01 - verify current inputs and identify the Angle blocker.
OWNER: Agent Local; MG reviews.
TASK FILE: `docs/logs/2026-09-12-lf01-local-angle-preflight-task.md`.
STATE: NOT EXECUTED by this planning change; eligible only after this task is merged to main and Founder dispatches it.
PERMISSION: read-only operational verification and CLI preflight, plus one sanitized evidence file/branch. No model generation, research, approval, DB mutation, migration or code fix.
SUCCESS: evidence identifies the rejecting layer or explicitly reports unknown, verifies/refutes expected inputs, and supports the next bounded MG decision.
STOP: after the report, or immediately on policy denial, unexpected state, missing permission or potential secret disclosure.

Do not activate LF-02/LF-03 merely because they appear in the plan. MG must review LF-01 and assign their exact scope, expected refs and budgets first.

## State that must not regress

Reuse the SAME fresh Journal run and input bundle identified in the checkpoint. Preserve independent VI/EN, approved Angle AND Outline, bounded revision, Assertion Audit v5 and Source-copy v2. Final hard gates and Founder approval remain unchanged. Non-critical warnings must be carried verbatim into the review package.

Implementation of the main Journal modules exists; the fresh candidate still needs real Angle, Outline, VI/EN, review, checks, package and final approval. Old O4 capability demonstrations are history, not this candidate's completion.

## Frozen until M1 passes

No additional provider/agent, generic framework, architecture redesign, automatic publishing/merge, WordPress work, repeated historical DB recovery or speculative CI work. No Assertion Audit v6 simply to pass one article. Only a demonstrated safety/data-integrity defect can interrupt the critical path.

## Evidence and next-state rule

LF-01 evidence target: `docs/logs/2026-09-12-lf01-local-angle-preflight-evidence.md` (not created or claimed by this plan). Keep raw private output local. MG verifies evidence before transitioning `docs/TASKS.md` and this view. GitHub holds decisions and sanitized observations; the local DB holds actual operational state.

`REAL RUN -> OBSERVE -> CLASSIFY -> SMALLEST PROVEN FIX -> REGRESSION -> RUN AGAIN`
